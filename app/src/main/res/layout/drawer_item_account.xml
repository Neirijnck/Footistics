<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:background="@drawable/account_pattern_inverse">

    <com.pkmmte.view.CircularImageView
        android:id="@+id/drawerItemPerson"
        android:layout_width="@dimen/person_icon_size"
        android:layout_height="@dimen/person_icon_size"
        android:layout_margin="@dimen/large_padding"
        android:src="?attr/drawableAccount"
        app:border="true"
        app:border_width="3dp"/>

    <TextView
        android:id="@+id/drawerItemUsername"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:paddingLeft="@dimen/large_padding"
        android:layout_marginBottom="@dimen/default_padding"
        android:singleLine="true"
        android:ellipsize="end"
        android:textColor="?attr/ftColorAccountTextDrawer"
        tools:text="Man, 21 jaar"/>

    <TextView
        android:id="@+id/drawerItemAccount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="@dimen/large_padding"
        android:layout_above="@id/drawerItemUsername"
        android:textStyle="bold"
        android:singleLine="true"
        android:ellipsize="end"
        android:textAllCaps="true"
        android:textColor="?attr/ftColorAccountTextDrawer"
        tools:text="Preben"/>

</RelativeLayout>