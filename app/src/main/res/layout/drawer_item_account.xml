<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:background="@drawable/account_pattern">

    <com.pkmmte.view.CircularImageView
        android:id="@+id/drawerItemPerson"
        android:layout_alignParentBottom="true"
        android:layout_width="@dimen/person_icon_size"
        android:layout_height="@dimen/person_icon_size"
        android:layout_marginLeft="@dimen/keyline"
        android:layout_marginRight="@dimen/keyline"
        android:layout_marginBottom="@dimen/large_padding"
        android:src="?attr/drawableAccount"
        app:border="true"
        app:border_width="4dp"/>

    <TextView
        android:id="@+id/drawerItemAccount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/drawerItemPerson"
        android:layout_toRightOf="@+id/drawerItemPerson"
        android:layout_toEndOf="@+id/drawerItemPerson"
        android:paddingTop="@dimen/inline_padding"
        android:textStyle="bold"
        android:singleLine="true"
        android:ellipsize="end"
        android:textAllCaps="true"
        tools:text="Preben"/>

    <TextView
        android:id="@+id/drawerItemUsername"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/drawerItemPerson"
        android:layout_toEndOf="@+id/drawerItemPerson"
        android:layout_below="@id/drawerItemAccount"
        android:singleLine="true"
        android:ellipsize="end"
        tools:text="Man, 21 jaar"/>

</RelativeLayout>